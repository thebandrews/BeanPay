define(['underscore', 'backbone', 'cachedAjax', 'BudgetCollection'],
    function(_, Backbone, cachedAjax, BudgetCollection) {

    return Backbone.Model.extend({

        initialize: function() {}

    });
});